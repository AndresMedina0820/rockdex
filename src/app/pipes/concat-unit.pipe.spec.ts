import { ConcatUnitPipe } from './concat-unit.pipe';

describe('ConcatUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ConcatUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
